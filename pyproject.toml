[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mardpg-uav"
version = "0.1.0"
description = "Multi-Agent UAV Routing with Recurrent DDPG"
dependencies = [
    "torch",
    "numpy",
    "gymnasium"
]
